<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="EcoPlatesMobile.Views.User.Pages.UserFavoritesPage"
             xmlns:component="clr-namespace:EcoPlatesMobile.Views.Components"
             xmlns:userComponent="clr-namespace:EcoPlatesMobile.Views.User.Components"
             Title="UserFavoritesPage">

    <Grid RowDefinitions="Auto, *">
        <Grid BackgroundColor="#007200"
              HeightRequest="87"
              ColumnDefinitions="*,Auto">
            <Label x:Name="lbTitle"
                   Text="Menga yoqqanlar"
                   FontFamily="Roboto"
                   TextColor="White"
                   FontSize="20"
                   VerticalOptions="Center"
                   HorizontalOptions="Center"/>
            <Image Source="favorites1.png"
                   WidthRequest="24"
                   HeightRequest="24"
                   Margin="0, 4.44, 15, 0"
                   Grid.Column="1"/>
        </Grid>

        <Grid RowDefinitions="50, *"
              Margin="16, 30, 16, 0"
              Grid.Row="1">
            <userComponent:TabSwitcherView x:Name="tabSwitcher"
                                           SwitchType="Classic"
                                           Tab1_Title="Sallar"
                                           Tab2_Title="Products"/>
            <CollectionView x:Name="listCompany" 
                            ItemsSource="{Binding Companies}"
                            Margin="0,20,0,0"
                            Grid.Row="1">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <userComponent:CompanyView/>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
            <CollectionView x:Name="listProduct"  
                            ItemsSource="{Binding Products}" 
                            IsVisible="False"
                            Margin="0,20,0,0"
                            Grid.Row="2">
                <CollectionView.ItemsLayout>
                    <GridItemsLayout Orientation="Vertical" 
                                     VerticalItemSpacing="10"
                                     HorizontalItemSpacing="10"
                                     Span="2"/>
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <userComponent:ProductView/>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </Grid>
    </Grid>
</ContentPage>